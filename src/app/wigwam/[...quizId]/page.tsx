import { Container } from '@/components/common';

type Props = {};

const QuizPage = (props: Props) => {
  return (
    <main>
      <Container>QuizPage</Container>
    </main>
  );
};

export default QuizPage;
